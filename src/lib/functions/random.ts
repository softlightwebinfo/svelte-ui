export const generateRandom = (highestNumber: number, lowestNumber: number) => Math.random() * (highestNumber - lowestNumber) + lowestNumber
export const generateRandomBetween = (min: number, max: number) => Math.floor(Math.random() * (max - min + 1)) + min;